[project]
name = "green-jobs-career-advisor"
version = "0.1.0"
description = "Autonomous AI agent for green career transition advice"
requires-python = ">=3.11"

dependencies = [
    "google-genai>=1.0.0",
    "ddgs>=0.1.0",
    "fpdf2>=2.7.0",
    "streamlit>=1.35.0",
    "python-dotenv>=1.0.0",
    "bcrypt>=4.0.0",
    "sqlite-utils>=3.35",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.4.0",
    "mypy>=1.10.0",
    "pre-commit>=3.7.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]